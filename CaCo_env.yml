name: CaCo
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - python=3.9
  - xz
  - numpy
  - pandas
  - matplotlib
  - tqdm
  - hmmer
  - prodigal          # optional, kept for fallback (but we use pyrodigal)
  - pip
  - pip:
    - pyrodigal       # fast Prodigal replacement
